

class Notifications {

  final String id, thumb, title, date;

  Notifications({required this.id, required this.thumb, required this.title, required this.date});
  //
  // Notification.fromJson(Map<String, dynamic> json):
  //       id = json['id'],
  //       image = json['image'],
  //       title = json['title'],
  //       date = json['date'];

}